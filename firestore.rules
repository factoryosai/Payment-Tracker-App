rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for direct login mode (no Firebase Auth required)
    match /{document=**} {
      allow read, write: if true; 
    }
  }
}
